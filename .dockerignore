# Build context is the repo root (see docker/docker-compose.yml). Keep local
# installs and secrets out of the image; deps are installed fresh inside it.
**/node_modules
**/dist
**/.env
**/.env.*
backend/prisma/dev.db*
**/*.log
.git
