# Direct-to-VM HTTPS for Option C (bypasses Cloudflare's proxy).
# Caddy auto-provisions a Let's Encrypt certificate for the domain and
# reverse-proxies to the backend container.
#
# Requires: the domain's DNS points straight at THIS VM (DNS-only / grey cloud)
# and ports 80 + 443 are open (Oracle Security List + the instance's iptables).
{
	email u6530338@au.edu
}

proctor.jesoas.org {
	reverse_proxy backend:3000
}
